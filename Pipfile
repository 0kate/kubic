[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
prompt-toolkit = "*"
pyyaml = "*"
rich = "*"
pygments = "*"

[requires]
python_version = "3.8"

[scripts]
tests = "python -m unittest discover tests"
build = "python setup.py sdist"
